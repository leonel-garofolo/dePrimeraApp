import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AsistentesForm extends StatefulWidget {
  AsistentesForm({Key key}) : super(key: key);

  @override
  _AsistentesFormState createState() => _AsistentesFormState();
}

class _AsistentesFormState extends State<AsistentesForm> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Asistentes'),
      ),
      body: Center(

      ),
    );
  }
}