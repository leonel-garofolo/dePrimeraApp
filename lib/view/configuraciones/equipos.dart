import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class EquiposForm extends StatefulWidget {
  EquiposForm({Key key}) : super(key: key);

  @override
  _EquiposFormState createState() => _EquiposFormState();
}

class _EquiposFormState extends State<EquiposForm> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Equipos'),
      ),
      body: Center(

      ),
    );
  }
}