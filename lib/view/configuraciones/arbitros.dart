import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ArbitrosForm extends StatefulWidget {
  ArbitrosForm({Key key}) : super(key: key);

  @override
  _ArbitrosFormState createState() => _ArbitrosFormState();
}

class _ArbitrosFormState extends State<ArbitrosForm> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Arbitros'),
      ),
      body: Center(

      ),
    );
  }
}